# SAC-BRC + LoRA

This folder contains the LoRA variant of the BRC critic used with SAC-style training. The LoRA path supports residual-block adapters and optional input/output adapter ablations.

Use `scripts/run_lora_example.sh` for a minimal runnable example. See the repository-level `README.md` for environment setup and reproducibility notes.
