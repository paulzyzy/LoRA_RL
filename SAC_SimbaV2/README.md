# SAC + SimbaV2 + LoRA

This folder contains the LoRA variant of SAC with SimbaV2 actor/critic networks. The default example enables critic residual-block LoRA and keeps actor LoRA disabled.

Use `scripts/run_lora_example.sh` for a minimal runnable example. See the repository-level `README.md` for environment setup and reproducibility notes.
